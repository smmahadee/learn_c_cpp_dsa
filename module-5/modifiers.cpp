#include <iostream>

using namespace std;

int main() {
    string str = "Mehedi";

    str = str + "Hasan" + "Himel";

    // str.erase();
    // str.pop_back();

    // str.erase(0, 7);

    // str.replace(6, 5, "Shohag");
    // cout<<str.find("Hasaj")<<endl;
    // cout<<str.npos;

    int t = str.find("sdaf");
    cout<<str.find("sdaf")<<endl<<str.npos;

    // str.insert(6 , " huma");

    // str.push_back('h');

    // cout << str << endl;

    // char a = '5';

    // cout<<(int) a;

    return 0;
}